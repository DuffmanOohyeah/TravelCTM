{
	"name": "travelctm-backend",
	"version": "1.0.0",
	"description": "Tech test for TravelCTM (backend)",
	"private": true,
	"main": "app.js",
	"scripts": {
		"dev": "nodemon ./app.js",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"author": "W. Boom (uk@boomedia.ca)",
	"license": "ISC",
	"devDependencies": {
		"nodemon": "^3.1.4"
	},
	"dependencies": {
		"cluster": "^0.7.7",
		"compression": "^1.7.4",
		"express": "^4.19.2",
		"fs": "^0.0.1-security",
		"mongodb": "^6.8.0",
		"redis": "^4.7.0"
	}
}
